<section class="full_post_container">
  <%= render partial: 'partials/post', locals: { post: @post, show_full_post: true } %>
  <div class="full_comments_container">
    <% @post.comments.each do |comment| %>
      <%= render partial: 'partials/comment', locals: { comment: comment } %>
    <% end %>
  </div>
</section>